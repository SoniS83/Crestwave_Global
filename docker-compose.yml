services:
  crestwave:
    build: .
    container_name: crestwave
    restart: always
    ports:
      - "80:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    env_file:
      - .env
